You are an expert Android Native Developer (Java/Kotlin).

Your task: Build a complete Android customer app that mirrors the website's customer portal exactly.

REQUIREMENTS:

1. DATABASE & BACKEND:
   - Use the EXACT SAME database schema, tables, and fields that the website uses
   - Implement proper REST API integration (if website has backend API)
   - OR implement SQLite locally with Room ORM for offline data storage
   - Sync data bidirectionally between local DB and backend
   - Handle authentication (login/signup) same as website

2. FEATURES (100% Complete):
   - List all customer features from the website
   - Implement EVERY single feature available to customers on website
   - No features should be skipped or simplified
   - Functionality must be 100% identical to website version
   - Only UI/UX should be optimized for mobile

3. TECHNICAL STACK (NATIVE ANDROID):
   - Language: Kotlin (preferred) or Java
   - Minimum SDK: Android 7.0 (API 24)
   - Target SDK: Latest (API 35+)
   - Architecture: MVVM with LiveData/StateFlow
   - Database: Room ORM for local storage
   - Networking: Retrofit + OkHttp for API calls
   - Async: Coroutines
   - UI: Material Design 3, AndroidX libraries
   - Dependency Injection: Hilt

4. APP STRUCTURE:
   - Splash/Loading Screen
   - Authentication Module (Login/Signup/Password Reset)
   - Dashboard/Home Screen (main entry point)
   - Navigation Drawer/Bottom Navigation (all features)
   - All customer features as separate screens/fragments
   - Settings & Profile Management
   - Offline support where applicable
   - Error handling & User feedback (Toast/Snackbar)

5. UI/UX FOR MOBILE:
   - Responsive design for different screen sizes
   - Touch-friendly buttons and inputs
   - Proper spacing and typography
   - Dark mode support
   - Smooth animations and transitions
   - Bottom sheets, dialogs instead of modals where appropriate
   - Proper back navigation

6. DATA SYNCHRONIZATION:
   - Auto-sync on app start
   - Background sync when possible
   - Handle network failures gracefully
   - Show sync status to user
   - Cache management

7. QUALITY REQUIREMENTS:
   - Handle all edge cases and errors
   - Input validation on all forms
   - Security: Store sensitive data securely (encrypted SharedPreferences/Keystore)
   - Proper Logcat logging for debugging
   - No hardcoded values (use strings.xml, colors.xml, etc.)

DELIVERABLES:
- Complete, runnable Android project
- All dependencies in build.gradle
- AndroidManifest.xml properly configured
- Drawable assets (use Android icons where possible)
- README with setup instructions
- Database schema documentation
- Feature checklist (verify all features implemented)

START BY:
1. Ask me about the website's features and database structure
2. Ask for the backend API endpoints (if any)
3. Ask for any specific design preferences
4. Then build the complete app